# LeetCode
刷题练习

# 模板

文件名

```
P${question.frontendQuestionId}$!velocityTool.camelCaseName(${question.titleSlug})
```
代码模板
```
package leetcode.editor.cn;
${question.content}
//scala:${question.title}
object P${question.frontendQuestionId}$!velocityTool.camelCaseName(${question.titleSlug}){
    def main(args: Array[String]){
        //TO TEST
        
    }
    ${question.code}
}

```